---
description: Show and verify the Cifra tamper-evident audit log
---

Run `cifra audit log show` to display the AI Privacy Shield audit entries, then `cifra audit log verify` to confirm the hash chain is intact. Report both results.
