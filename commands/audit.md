---
description: Show and verify the Envault tamper-evident audit log
---

Run `envault audit log show` to display the AI Privacy Shield audit entries, then `envault audit log verify` to confirm the hash chain is intact. Report both results.
